# Streamlit Coin App

Aplicación de ejemplo para simular lanzamientos de una moneda usando Streamlit.

## Estado
Aplicación desplegada en Render: `https://streamlit-coin-app-ykpw.onrender.com`

## Uso local (recomendado con entorno virtual)

1. Crear y activar un entorno virtual (opcional pero recomendado):
```bash
python -m venv .venv
.venv\Scripts\activate   # Windows PowerShell
